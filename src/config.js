// Public browser configuration. The anon key is safe to expose when RLS is enabled.
// Replace these two values after creating the Supabase project.
window.NTUCUP_CONFIG = {
    supabaseUrl: "",
    supabaseAnonKey: ""
};
